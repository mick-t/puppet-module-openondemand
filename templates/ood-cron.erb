#!/bin/bash

PATH=/sbin:/bin:/usr/sbin:/usr/bin
<%= scope['openondemand::nginx_stage_clean_cron_schedule'] %> root [ -f <%= scope['openondemand::nginx_stage_cmd'] %> ] && <%= scope['openondemand::nginx_stage_cmd'] %> nginx_clean 2>&1 | logger -t nginx_clean
